<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cv_genre_holder"
    android:layout_width="100dp"
    android:layout_height="110dp"
    android:layout_marginEnd="3dp"
    android:layout_marginBottom="3dp"
    android:foreground="?attr/selectableItemBackground"
    android:layout_marginTop="3dp"
    android:elevation="18dp"
    android:padding="3dp"
    app:cardCornerRadius="15dp">

    <ImageView
        android:id="@+id/iv_genre_img"
        android:layout_width="60dp"
        android:layout_gravity="center|top"
        android:layout_height="80dp"
        android:backgroundTint="@color/list_layout_bg"
        android:src="@drawable/ic_genre_action" />

    <com.example.moviereviewapp.ui.view.AppTextView
        android:id="@+id/tv_genre_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center"
        android:layout_marginBottom="10dp"
        android:text="äction"
        android:textColor="@color/tmdb_secondary"
        android:textSize="14sp" />
</androidx.cardview.widget.CardView>